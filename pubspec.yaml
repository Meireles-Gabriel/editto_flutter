name: editto_flutter
description: "Éditto Magazine."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  google_fonts: 
  animate_do: 
  flutter_riverpod: 
  shared_preferences: ^2.5.3 
  firebase_core: 
  cloud_firestore: 
  firebase_auth: 
  firebase_storage: 
  flutter_stripe: 11.4.0
  flutter_stripe_web: 
  cached_network_image: 
  printing: 
  pdf: 
  flutter_pdfview: ^1.4.0 
  http: 
  flutter_launcher_icons: 
  flutter_native_splash: 
  intl: ^0.20.2
  path_provider: ^2.1.5
  

flutter_native_splash:
  color: "#2F3844"
  image: "assets/icon.png"
  android: true
  ios: true

flutter_icons:
  image_path: "assets/icon.png"
  ios: true
  remove_alpha_ios: true
  android: "launcher_icon"
  adaptive_icon_background: "#2F3844"
  adaptive_icon_foreground: "assets/icon_android.png"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/